import React from 'react';

export const WeekFormat = React.createClass({
    render: function() {
        return (
            <input
                type='text'
                placeholder='445'
                defaultValue='445'
            />
        );
    }
});
